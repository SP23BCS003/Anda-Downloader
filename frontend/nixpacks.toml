# Use Node.js 24 (required by SvelteKit packages: >=24)
[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node build/index.js"
